# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
vite.config.js.timestamp-*

# Rust / Tauri
src-tauri/target/
src-tauri/gen/schemas/

# MSIX packaging — staged payload and the generated package
dist-msix/
*.msix
*.msixbundle
*.appx

# Signing material — NEVER commit a certificate or private key
*.pfx
*.p12
*.key
devcert*

# Logs & env
*.log
.env
.env.*
!.env.example

# Personal / private notes (never commit — this repo is public)
future-steps.md

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
Thumbs.db
